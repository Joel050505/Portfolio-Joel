export default function SmallSkill({ skill }) {
  return (
    <span className="bg-blue-50 text-blue-600 dark:bg-slate-900 dark:text-white px-2 py-1 rounded-full text-xs font-semibold">
      {skill}
    </span>
  );
}
